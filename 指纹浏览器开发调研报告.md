# 指纹浏览器（Anti-Detect Browser）开发调研报告

> 调研日期：2026-08-16
> 目标：为"从零开发一个指纹浏览器"梳理知识体系、开源项目、商业参照与技术路线。
>
> **进度**：Phase 1（MVP）、Phase 2（专业级）、Phase 3（团队与规模化）均已完成的开发与测试，见 `README.md` 与 `scripts/` 四套测试脚本。
> - Phase 1：Camoufox 内核集成、每环境固定指纹（跨重启稳定）、代理绑定与 geoip、本地自动化 API、中文管理界面
> - Phase 2：双指纹模式（合成/真实预设）+ 一致性体检、批量操作、导入导出与整机备份、RPA 任务引擎、API Key 认证、代理密码加密、审计日志
> - Phase 3：fingerprint-chromium 第三内核（Chromium 定制 + CDP 直连）、指纹矩阵风控（分布/查重/矩阵感知重生成）、团队协作（多成员/角色/数据隔离）、自托管同步服务器（push/pull/LWW/删除传播）、定时调度 RPA、Webhook 通知、环境使用统计

---

## 1. 什么是指纹浏览器

网站通过浏览器暴露的数十项特征（Canvas 像素、WebGL 渲染、字体、时区、硬件参数……）组合出近乎唯一的"浏览器指纹"，用于识别/追踪访客。指纹浏览器（反检测浏览器）让每个"环境实例（profile）"呈现一套**真实且自洽**的独立指纹，从而让多个会话被识别为多台不同的真实设备。

**典型合法用途**：电商/社媒多店铺与多账号运营、广告验证（检查不同地区的投放展示）、价格与市场数据采集、爬虫与自动化测试（QA）、隐私保护。

---

## 2. 商业产品全景（对标参照）

### 国际
| 产品 | 内核 | 特点 | 官网 |
|---|---|---|---|
| Multilogin | Mimic（Chromium 改）/ Stealthfox（Firefox 改） | 行业标杆，内核级改写，宣称指纹替换发生在内核内部而非 JS 注入层 | multilogin.com |
| GoLogin | Chromium 改 | 云端 profile 同步 | gologin.com |
| AdsPower | Chromium 改 | Local API + RPA，国内外通吃 | adspower.com |
| Dolphin Anty / Incogniton / Kameleo / Undetectable / OctoBrowser / MoreLogin / Linken Sphere / VMLogin | 多为 Chromium 改 | 各有侧重的多账号管理 | — |

### 国内
比特浏览器（BitBrowser）、紫鸟浏览器（跨境电商向）、候鸟浏览器、Hubstudio、拉力猫等。国内产品普遍主打：批量环境管理、团队协作、平台账号养号、与国产代理/IP 资源集成。

### 通用产品架构（我们要做的东西）
1. **Profile 管理**：每个环境独立的 Cookie、LocalStorage、IndexedDB、扩展、书签（磁盘级目录隔离）。
2. **指纹配置**：为每个 profile 生成或指定一套指纹（UA、Canvas/WebGL、字体、时区、语言、分辨率、硬件并发数……），可导出/迁移/锁定。
3. **代理绑定**：每个 profile 绑定 HTTP/SOCKS5 代理，浏览器所有流量（含 WebRTC）走代理且不泄露真实 IP。
4. **启动器（Launcher）**：以指定参数（user-data-dir、proxy、指纹配置）拉起内核进程，并暴露自动化调试端口。
5. **自动化 API**：本地 HTTP API（参考 AdsPower Local API：`/api/v1/browser/start` 返回 CDP `wsEndpoint`，供 Selenium/Puppeteer/Playwright 连接）+ RPA。
6. **协作层**：环境/代理/账号的分组、权限、云端同步（商业化必需）。

**参考链接**：
- Multilogin 内核原理：https://multilogin.com/antidetect/antidetect-browser/ 、Mimic 介绍 https://multilogin.com/blog/the-most-advanced-browser-fingerprinting-protection-ever-created-enter-mimic/
- AdsPower Local API 文档：https://localapi-doc-en.adspower.com/ 、https://github.com/AdsPower/localAPI
- 反检测浏览器工作原理综述：https://app.cyberyozh.com/blog/anti-detection-browsers-how-that-work/ （CyberYozh）

---

## 3. 指纹信号面（要伪造/自洽的所有维度）

### 主动信号（需脚本计算，最易出独特值）
| 信号 | 原理 | 伪造难点 |
|---|---|---|
| Canvas | 离屏绘制文字/图形后读像素做哈希；GPU/驱动/字体光栅化差异导致像素不同 | 加噪声易被"同一页面两次读取不一致"识破；需稳定且随 profile 变化 |
| WebGL / WebGPU | UNMASKED_RENDERER_WEBGL 厂商串 + 渲染 3D 场景的差异 | 渲染器串要与 UA 声称的 OS 匹配（Win UA 配 Apple GPU = 立即穿帮） |
| AudioContext | OfflineAudioContext 处理测试信号，DSP 差异产生独特频谱输出 | 同上，噪声需确定性 |
| 字体枚举 | 无法直接列字体，靠测量文本宽度/glyph 尺寸推断已装字体 | 需要伪造一份与目标 OS 匹配的"合理字体集"及其度量 |
| ClientRects / CSS | getClientRects 等布局测量受字体渲染影响 | 与字体伪造联动 |

### 被动信号（页面环境直接可读）
- **Navigator**：userAgent、platform、hardwareConcurrency（CPU 核数）、deviceMemory、languages、plugins/mimeTypes、webdriver（必须为 false 或不存在）、userAgentData（UA-CH / Sec-CH-UA 请求头，与 UA 联动）。
- **Screen**：分辨率、色深、devicePixelRatio、可用区域（与 GPU/显示器合理性联动）。
- **环境一致性**：时区（Date/Intl）、语言（Accept-Language 头 vs navigator.languages）、地理位置，三者必须与代理 IP 所属地一致。
- **其它**：MediaDevices 列表、speechSynthesis voices、触摸点数、传感器、键盘布局、Math/JS 引擎行为差异。

### 网络层指纹（常被忽视但致命）
- **TLS 指纹 JA3/JA4**：ClientHello 的密码套件/扩展顺序；用 Go/Python 冒充 Chrome UA 但 TLS 是 curl → 秒判 bot。Cloudflare 官方用于 Bot Management。
- **HTTP/2 指纹**：SETTINGS 帧、HEADERS 顺序、PRIORITY 结构（Akamai h2 指纹）。
- **Header 顺序与大小写**。
- 结论：**只要用真浏览器内核发请求（而不是脚本 HTTP 客户端），网络层指纹天然正确**——这是"内核路线"的一大隐性优势。

### WebRTC
- STUN 请求可绕过代理暴露真实公网 IP（经典 leak）。对策：内核层伪造/禁用 candidate，或强制 WebRTC 走代理（Chromium 的 `--force-webrtc-ip-handling-policy`、`--disable-webrtc` 等）。
- 测试：https://browserleaks.com/webrtc

**核心心法**：检测方查的往往不是"指纹罕见"，而是"**撒谎的痕迹**"——各信号之间互相矛盾（CreepJS 的 lies 机制）、iframe 与主 frame 不一致、Worker 内取值不同、`toString` 被改写、属性描述符异常。**伪装的理想是成为一台真实存在过的设备。**

---

## 4. 检测端（你的对手与测试工具）

| 工具 | 说明 |
|---|---|
| CreepJS（https://github.com/abrahamjuliot/creepjs ，在线 https://abrahamjuliot.github.io/creepjs） | 最难对付的开源检测：lies 检测（发现 API 被篡改）、trash/lies 扣分、trust 分数。对反检测工具的启示：宁可指纹"罕见但诚实"，不可"常见但撒谎" |
| FingerprintJS 开源版（https://github.com/fingerprintjs/fingerprintjs） | 客户端指纹库，商用 Pro 版增加服务端信号与识别精度 |
| BrowserLeaks（https://browserleaks.com） | 全信号分项测试（canvas/webgl/fonts/webrtc/tls） |
| amiunique（https://amiunique.org/fp）、EFF Cover Your Tracks（https://coveryourtracks.eff.org） | 学术/EFF 维度的指纹独特性测试 |
| Pixelscan、IPHey、bot.sannysoft.com | 反检测行业通用自检（环境一致性、bot 特征） |

深度阅读：
- CreepJS 判谎机制解析：https://feder-cr.github.io/invisible_playwright/creepjs-explained.html
- 检测爬虫服务的经典文：https://incolumitas.com/2021/03/11/detecting-scraping-services/
- TLS/H2 指纹原理：https://scrapfly.io/web-scraping-tools/ja3-fingerprint 、https://www.browserless.io/blog/tls-fingerprinting-explanation-detection-and-bypassing-it-in-playwright-and-puppeteer 、Cloudflare 官方文档 https://developers.cloudflare.com/bots/additional-configurations/ja3-ja4-fingerprint/

---

## 5. 四条技术路线对比

| 路线 | 代表 | 隐蔽性 | 开发成本 | 长期维护 | 适用阶段 |
|---|---|---|---|---|---|
| **A. 内核源码 patch**（改 Chromium/Firefox C++ 后自编译） | Multilogin、Camoufox、fingerprint-chromium | ★★★★★（C++ 层改值，无 JS 痕迹；TLS/H2 天然正确） | 高（需 C++、编译环境、跟随上游版本） | 高（Chromium 每 4 周一版，需持续 rebase） | 最终形态 |
| **B. JS 注入**（CDP `Page.addScriptToEvaluateOnNewDocument` / 扩展注入 hook 脚本） | puppeteer-extra-stealth、fingerprint-suite injector | ★★☆（toString/描述符/iframe/Worker 一致性全是坑，已被现代检测吊打） | 低 | 低 | MVP、原型验证 |
| **C. 动态库注入**（Windows DLL 注入 chrome.exe / Linux LD_PRELOAD） | 部分国产厂商的做法 | ★★★★（native 层 hook，比 JS 隐蔽，比改源码轻） | 中高 | 中（Chromium 更新可能改函数签名） | 折中方案 |
| **D. 站在巨人肩上**（用 Camoufox / fingerprint-chromium 现成内核 + 自研产品层） | 基于 Camoufox 做管理界面 | ★★★★★（取决于所选内核） | 低—中 | 低（跟随上游项目） | **推荐的起步路线** |

各路线的关键事实：
- **Camoufox 的选择理由**（对我们有参考价值）：Chromium 与 Chrome 存在可检测差异、CDP 本身是检测目标、Firefox 的反指纹研究（Tor/Arkenfox）更充分；它用 Playwright Juggler 协议而非 CDP，Page Agent 跑在沙箱世界。
- **CDP 自动化的固有泄露**：Playwright/Puppeteer 启动时会发 `Runtime.enable`、`Target.*` 等特征命令；Patchright 的做法就是 patch 掉这些信号；nodriver 干脆不走 webdriver、直连 CDP。
- `navigator.webdriver`、`--enable-automation` 提示条、"Chrome is being controlled" 等基础特征必须处理。

---

## 6. 开源项目清单（按用途分类）

### 6.1 完整反检测内核（可直接用/参考其 patch）
| 项目 | 地址 | 说明 |
|---|---|---|
| **Camoufox** | https://github.com/daijro/camoufox （文档 https://camoufox.com/） | Firefox debloat 构建 + C++ 层指纹改写；覆盖 navigator/screen/WebGL/字体/locale/语音/WebRTC IP；指纹由 BrowserForge 按真实流量分布生成保证自洽；兼容 Playwright；**v146.0.1-beta.25（2026-01）起源码全开**（早期 Canvas 补丁闭源）。当前开源界最强 |
| **fingerprint-chromium** | https://github.com/adryfish/fingerprint-chromium | Chromium patch 路线的开源实现（中文 README），可参考其改了哪些模块、如何配置每 profile 指纹 |
| **r0chrome** | https://gitee.com/mywebgame/r0chrome | 国人维护的定制 Chromium，GPU/WebGL 等随机指纹，面向爬虫/JS 逆向 |
| **Donut Browser** | https://github.com/zhom/donutbrowser | Chromium 隔离多 profile 反检测浏览器（含 GUI），结构接近"轻量版比特浏览器"，适合研究产品层实现 |

### 6.2 指纹生成与注入（JS 路线的最佳实践）
| 项目 | 地址 | 说明 |
|---|---|---|
| **fingerprint-suite** | https://github.com/apify/fingerprint-suite | Apify 出品：fingerprint-generator（按真实统计分布生成自洽指纹）+ fingerprint-injector（注入 Playwright/Puppeteer） |
| **BrowserForge** | https://github.com/daijro/browserforge | 同类指纹生成库（Python），Camoufoox 御用 |
| **puppeteer-extra + stealth** | https://github.com/berstend/puppeteer-extra | 老牌 evasions 集合（chrome.runtime、navigator.webdriver、languages 等 12+ 项），学习 hook 写法的教材，但已扛不住现代检测 |

### 6.3 反检测自动化库（做自动化 API 时参考）
| 项目 | 说明 |
|---|---|
| **nodriver**（ultrafunkamsterdam） | undetected-chromedriver 的继任者：Python、无 webdriver、直连 CDP |
| **Patchright** | Playwright fork，patch 掉 CDP 泄露（Runtime.enable 等）；要 Playwright API 就选它 |
| **SeleniumBase UC Mode** | 维护活跃，Cloudflare 通用性口碑好 |
| **undetected-chromedriver** | 开山之作，维护放缓 |
| 2026 横评 | https://scrapfly.io/blog/posts/best-stealth-browsers 、https://ianlpaterson.com/blog/anti-detect-browser-benchmark-patchright-nodriver-curl-cffi/ （7 工具 651 次判定）、演进史 https://blog.castle.io/from-puppeteer-stealth-to-nodriver-how-anti-detect-frameworks-evolved-to-evade-bot-detection/ |

### 6.4 检测/测试端
CreepJS、FingerprintJS、browserleaks、amiunique、 Pixelscan、IPHey、bot.sannysoft.com（见第 4 节）。

### 6.5 相关 GitHub Topic（持续发现新项目）
- https://github.com/topics/antidetect-browser
- https://github.com/topics/anti-detect
- https://github.com/topics/browser-fingerprinting
- https://github.com/topics/browser-fingerprint-bypass

---

## 7. 中文实战资料（Chromium 改内核路线）

- 指纹浏览器开发从原理到 Chromium 源码级实现：https://nexbrowser.net/blog/79.html
- 魔改 Chromium 源码——Canvas 指纹修改系列（掘金，重点讲随机值一致性问题）：https://juejin.cn/post/7491928461277741108
- CloakBrowser：编译期 Canvas 指纹定制（CSDN）：https://blog.csdn.net/weixin_42557907/article/details/147342815
- Chromium 源码改造实战：构建动态指纹特性的浏览器引擎：https://blog.csdn.net/weixin_29021195/article/details/157418871
- 编写自己的指纹浏览器以及指纹信息的来源（知乎）：https://zhuanlan.zhihu.com/p/494770064
- 指纹浏览器「内核秘密」：深度定制 Chromium 强在哪（SegmentFault）：https://segmentfault.com/a/1190000047860084
- 社区讨论（LINUX DO）：https://linux.do/t/topic/16896
- B 站：源码层 SSL/TLS/JA3/JA4 指纹修改：https://www.bilibili.com/video/BV1VH4y1G7RH/

### Chromium 编译（路线 A 的地基）
- 官方 Windows 编译指南：https://chromium.googlesource.com/chromium/src/+/main/docs/windows_build_instructions.md
- 硬件要求：x86-64、16GB+ 内存（8GB 是底线）、100GB+ NTFS 磁盘、VS + Win10/11 SDK、depot_tools；全量编译普通机器需数小时，核心越多越快
- 社区教程：https://swko.hashnode.dev/how-to-build-chromium-on-windows

---

## 8. 建议开发路径

**Phase 0 — 知识准备（1~2 周）**
通读 Camoufox 文档 + CreepJS 判文 + Castle.io 演进史；用 BrowserLeaks/CreepJS/Pixelscan 测一遍自己的日常浏览器，建立"信号面"直觉。

**Phase 1 — MVP（2~6 周）：产品层起步，不自研内核**
- 内核：直接集成 **Camoufox**（Python + Playwright）或 **fingerprint-chromium**（Chromium 系）。
- 产品层：Tauri/Electron 管理界面：profile 增删改查（每个 profile = 独立数据目录 + 指纹配置 + 代理配置）。
- 代理：HTTP/SOCKS5 每环境绑定；WebRTC 策略化处理（禁用/伪造）。
- 自检：内置一键打开 CreepJS/BrowserLeaks/Pixelscan 做回归。
- 自动化：本地 HTTP API（仿 AdsPower：`start` 返回 CDP/Juggler endpoint），即可对接 Selenium/Puppeteer/Playwright 生态。

**Phase 2 — 差异化（1~3 月）**
- 指纹库：采集真实设备数据建立指纹生成分布（学 fingerprint-generator/BrowserForge 的做法），保证跨信号自洽（OS↔GPU↔字体↔时区↔语言）。
- 团队协作/云同步/环境分组；RPA 或脚本市场。

**Phase 3 — 自研内核（长期）**
- 以 fingerprint-chromium / r0chrome 的 patch 为起点维护自己的 Chromium fork；建立"每 4 周跟随上游"的 rebase 流水线。
- 或走 Camoufox 同源的 Firefox 路线（反检测研究资料更多、CDP 检测免疫）。
- 网络层如需自定义 HTTP 客户端（非浏览器场景），参考 curl-impersonate / Surf（Go）做 JA3/H2 一致性。

**技术栈建议**：产品层 C# (WPF/WinUI) 或 Tauri (Rust+Web) 或 Electron；控制层 Python（Camoufox/nodriver 生态）或 Node（Puppeteer 生态）；内核 C++。

---

## 9. 合规提醒

指纹浏览器本身是中性技术（商业产品众多），但用途决定法律风险：多账号运营需遵守各平台服务条款；数据采集需遵守目标网站 ToS、robots 与当地数据法规；**严禁**用于账号买卖、欺诈、洗钱营销、规避司法封禁等场景。商用产品出海还应考虑出口管制与隐私合规（GDPR 等）。
